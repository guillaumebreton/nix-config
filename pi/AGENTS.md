# Global context

## About me

Full-stack developer working primarily in Go, TypeScript, and Python.
Editor: Zed. Shell: zsh. OS: macOS (aarch64).

## Preferences

- Keep responses concise — prefer code over explanation unless I ask
- Prefer explicit over clever
- When editing files make surgical changes, don't rewrite things that don't need changing
- Always run the build/tests after making changes to verify nothing is broken
- Commit messages: imperative mood, lowercase, no period (`fix: thing` not `Fix thing.`)

## Stack

- **Go** — standard library preferred, minimal dependencies
- **TypeScript** — strict mode, no `any`, prefer `zod` for validation
- **Python** — prefer `uv` for deps, `ruff` for formatting/linting
- **Nix / home-manager** — this machine is managed via nix-config at ~/Workspaces/nix-config

## Project conventions

- Use `fd`, `rg` instead of `find`, `grep`
- Use `bat` instead of `cat`
- Prefer `jq` for JSON manipulation
